version: 2

updates:
  # Keep the template repo itself low-noise.
  # Client repos should run scripts/init-project.sh to apply stack-specific updates.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "security"
